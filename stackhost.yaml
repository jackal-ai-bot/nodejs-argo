# stackhost.yaml - Simple configuration
runtime:
  image: ghcr.io/eooce/nodejs-sbx:latest
commands:
  package: ""
  build:
    - "npm install"
  start: "npm run start"
# Optional settings
repository:
  branch: main
  auto_deploy: true
