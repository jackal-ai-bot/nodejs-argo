# stackhost.yaml - Simple configuration
runtime:
  image: ghcr.io/momoaka-ai/goxhttp:latest
commands:
  package: ""
  build:
    - "npm install"
  start: "npm run start"
# Optional settings
repository:
  branch: main
  auto_deploy: true
