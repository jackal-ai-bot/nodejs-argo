# stackhost.yaml - Simple configuration
runtime:
  image: ghcr.io/jackal-ai-bot/stackhost:latest
commands:
  package: ""
  build:
    - "npm install"
  start: "npm run start"
# Optional settings
repository:
  branch: main
  auto_deploy: true
